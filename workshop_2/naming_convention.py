"""
Docstring

1. use consize and descriptive name 
2. Only _ underscore is valid special character (" ", (, % are illegal character)
3. Don't use reserved keywords
4. we use snake_case and SCREAMING_SNAKE_CASE (constants)
"""
student_name = "George"

name_of_the_student_length = len(student_name) # Bad practice
s_n_l = len(student_name) # Bad practice
student_name_length = len(student_name) # better

# int = "hello"
# only _ (underscords)

# l = 1
# O = 0
